import Splash from './Splash';

const meta = {
  title: 'Pages/Splash',
  component: Splash,
  parameters: {
    viewport: {
      defaultViewport: 'iPhone14',
    },
  },
};

export default meta;

export const Default = {};