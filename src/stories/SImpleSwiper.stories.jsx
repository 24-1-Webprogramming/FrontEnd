import { parameters } from '../../.storybook/preview';
import SimpleSwiper from './SImpleSwiper';

const meta = {
  component: SimpleSwiper,
  tags: ['autodocs'],
  parameters: {
    layout: 'centered',
  },
};

export default meta;

export const Default = {};