import { parameters } from '../../.storybook/preview';
import CardSlides from './CardSlides';

const meta = {
  title: 'Components/CardSlides',
  component: CardSlides,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
};

export default meta;

export const Default = {};